# placeholder for ffmpeg pipeline to generate the video from the haikus and episodes.

# Flow:
# 1. See if possible to generate pipeline from json files to video using ffmpeg.
# 2. If not, create a pipeline, where user screen recording is used to generate the video.
# 3. Output should be a video file in the assets/tmChronicles directory.
# 4. Make things configurable using config.json
